# IJVM simulator

Maak een IJVM programma om te simuleren
Bijvoorbeeld voor de volgende Python code:
```python
print(7*(8+4))
```

of deze Python functie met aanroep:

```python
def fib(n) {
  if n < 2:
    return 1
  else:
    return fib(n-1) + fib(n-2)
}

print(fib(10))
```